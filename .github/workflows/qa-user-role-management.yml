# .github/workflows/qa-user-role-management.yml

name: QA - User Role Management Service

on:
  push:
    branches:
      - qa
    paths:
      - 'apps/user/user-role-management-service/**'
      - '.github/workflows/qa-user-role-management.yml'
  pull_request:
    branches:
      - qa
    paths:
      - 'apps/user/user-role-management-service/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r apps/user/user-role-management-service/requirements.txt
          pip install pytest

      - name: Run tests
        run: |
          cd apps/user/user-role-management-service
          PYTHONPATH=. pytest tests/

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: rommela462
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t rommela462/user-role-management-service:qa-latest ./apps/user/user-role-management-service
          docker push rommela462/user-role-management-service:qa-latest
